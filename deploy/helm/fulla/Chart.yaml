apiVersion: v2
name: fulla
description: >-
  Fulla OAuth2/OIDC identity provider: Drogon C++ backend, user-facing
  SPA and admin console, with a pre-install/pre-upgrade schema-migration
  hook Job (fulla-server --migrate-only).
type: application
# Chart version tracks the app: both bump together on release (Task 37).
version: 1.2.0
appVersion: "1.2.0"
keywords:
  - oauth2
  - oidc
  - identity-provider
sources:
  - https://github.com/voidvec/fulla
