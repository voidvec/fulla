/**
 *
 *  Oauth2UserConsents.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "Oauth2UserConsents.h"
#include "Oauth2Clients.h"
#include "Oauth2Scopes.h"
#include "Users.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::fulla_db;

const std::string Oauth2UserConsents::Cols::_id = "\"id\"";
const std::string Oauth2UserConsents::Cols::_internal_user_id = "\"internal_user_id\"";
const std::string Oauth2UserConsents::Cols::_client_id = "\"client_id\"";
const std::string Oauth2UserConsents::Cols::_scope_name = "\"scope_name\"";
const std::string Oauth2UserConsents::Cols::_granted_at = "\"granted_at\"";
const std::string Oauth2UserConsents::primaryKeyName = "id";
const bool Oauth2UserConsents::hasPrimaryKey = true;
const std::string Oauth2UserConsents::tableName = "\"oauth2_user_consents\"";

const std::vector<typename Oauth2UserConsents::MetaData> Oauth2UserConsents::metaData_={
{"id","int32_t","integer",4,1,1,1},
{"internal_user_id","int32_t","integer",4,0,0,1},
{"client_id","std::string","character varying",50,0,0,0},
{"scope_name","std::string","character varying",100,0,0,0},
{"granted_at","::trantor::Date","timestamp without time zone",0,0,0,0}
};
const std::string &Oauth2UserConsents::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
Oauth2UserConsents::Oauth2UserConsents(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
        if(!r["id"].isNull())
        {
            id_=std::make_shared<int32_t>(r["id"].as<int32_t>());
        }
        if(!r["internal_user_id"].isNull())
        {
            internalUserId_=std::make_shared<int32_t>(r["internal_user_id"].as<int32_t>());
        }
        if(!r["client_id"].isNull())
        {
            clientId_=std::make_shared<std::string>(r["client_id"].as<std::string>());
        }
        if(!r["scope_name"].isNull())
        {
            scopeName_=std::make_shared<std::string>(r["scope_name"].as<std::string>());
        }
        if(!r["granted_at"].isNull())
        {
            auto timeStr = r["granted_at"].as<std::string>();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 5 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
        index = offset + 0;
        if(!r[index].isNull())
        {
            id_=std::make_shared<int32_t>(r[index].as<int32_t>());
        }
        index = offset + 1;
        if(!r[index].isNull())
        {
            internalUserId_=std::make_shared<int32_t>(r[index].as<int32_t>());
        }
        index = offset + 2;
        if(!r[index].isNull())
        {
            clientId_=std::make_shared<std::string>(r[index].as<std::string>());
        }
        index = offset + 3;
        if(!r[index].isNull())
        {
            scopeName_=std::make_shared<std::string>(r[index].as<std::string>());
        }
        index = offset + 4;
        if(!r[index].isNull())
        {
            auto timeStr = r[index].as<std::string>();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }

}

Oauth2UserConsents::Oauth2UserConsents(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 5)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        dirtyFlag_[0] = true;
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            id_=std::make_shared<int32_t>((int32_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            internalUserId_=std::make_shared<int32_t>((int32_t)pJson[pMasqueradingVector[1]].asInt64());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            clientId_=std::make_shared<std::string>(pJson[pMasqueradingVector[2]].asString());
        }
    }
    if(!pMasqueradingVector[3].empty() && pJson.isMember(pMasqueradingVector[3]))
    {
        dirtyFlag_[3] = true;
        if(!pJson[pMasqueradingVector[3]].isNull())
        {
            scopeName_=std::make_shared<std::string>(pJson[pMasqueradingVector[3]].asString());
        }
    }
    if(!pMasqueradingVector[4].empty() && pJson.isMember(pMasqueradingVector[4]))
    {
        dirtyFlag_[4] = true;
        if(!pJson[pMasqueradingVector[4]].isNull())
        {
            auto timeStr = pJson[pMasqueradingVector[4]].asString();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }
}

Oauth2UserConsents::Oauth2UserConsents(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("id"))
    {
        dirtyFlag_[0]=true;
        if(!pJson["id"].isNull())
        {
            id_=std::make_shared<int32_t>((int32_t)pJson["id"].asInt64());
        }
    }
    if(pJson.isMember("internal_user_id"))
    {
        dirtyFlag_[1]=true;
        if(!pJson["internal_user_id"].isNull())
        {
            internalUserId_=std::make_shared<int32_t>((int32_t)pJson["internal_user_id"].asInt64());
        }
    }
    if(pJson.isMember("client_id"))
    {
        dirtyFlag_[2]=true;
        if(!pJson["client_id"].isNull())
        {
            clientId_=std::make_shared<std::string>(pJson["client_id"].asString());
        }
    }
    if(pJson.isMember("scope_name"))
    {
        dirtyFlag_[3]=true;
        if(!pJson["scope_name"].isNull())
        {
            scopeName_=std::make_shared<std::string>(pJson["scope_name"].asString());
        }
    }
    if(pJson.isMember("granted_at"))
    {
        dirtyFlag_[4]=true;
        if(!pJson["granted_at"].isNull())
        {
            auto timeStr = pJson["granted_at"].asString();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }
}

void Oauth2UserConsents::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 5)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            id_=std::make_shared<int32_t>((int32_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            internalUserId_=std::make_shared<int32_t>((int32_t)pJson[pMasqueradingVector[1]].asInt64());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            clientId_=std::make_shared<std::string>(pJson[pMasqueradingVector[2]].asString());
        }
    }
    if(!pMasqueradingVector[3].empty() && pJson.isMember(pMasqueradingVector[3]))
    {
        dirtyFlag_[3] = true;
        if(!pJson[pMasqueradingVector[3]].isNull())
        {
            scopeName_=std::make_shared<std::string>(pJson[pMasqueradingVector[3]].asString());
        }
    }
    if(!pMasqueradingVector[4].empty() && pJson.isMember(pMasqueradingVector[4]))
    {
        dirtyFlag_[4] = true;
        if(!pJson[pMasqueradingVector[4]].isNull())
        {
            auto timeStr = pJson[pMasqueradingVector[4]].asString();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }
}

void Oauth2UserConsents::updateByJson(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("id"))
    {
        if(!pJson["id"].isNull())
        {
            id_=std::make_shared<int32_t>((int32_t)pJson["id"].asInt64());
        }
    }
    if(pJson.isMember("internal_user_id"))
    {
        dirtyFlag_[1] = true;
        if(!pJson["internal_user_id"].isNull())
        {
            internalUserId_=std::make_shared<int32_t>((int32_t)pJson["internal_user_id"].asInt64());
        }
    }
    if(pJson.isMember("client_id"))
    {
        dirtyFlag_[2] = true;
        if(!pJson["client_id"].isNull())
        {
            clientId_=std::make_shared<std::string>(pJson["client_id"].asString());
        }
    }
    if(pJson.isMember("scope_name"))
    {
        dirtyFlag_[3] = true;
        if(!pJson["scope_name"].isNull())
        {
            scopeName_=std::make_shared<std::string>(pJson["scope_name"].asString());
        }
    }
    if(pJson.isMember("granted_at"))
    {
        dirtyFlag_[4] = true;
        if(!pJson["granted_at"].isNull())
        {
            auto timeStr = pJson["granted_at"].asString();
            struct tm stm;
            memset(&stm,0,sizeof(stm));
            auto p = strptime(timeStr.c_str(),"%Y-%m-%d %H:%M:%S",&stm);
            time_t t = mktime(&stm);
            size_t decimalNum = 0;
            if(p)
            {
                if(*p=='.')
                {
                    std::string decimals(p+1,&timeStr[timeStr.length()]);
                    while(decimals.length()<6)
                    {
                        decimals += "0";
                    }
                    decimalNum = (size_t)atol(decimals.c_str());
                }
                grantedAt_=std::make_shared<::trantor::Date>(t*1000000+decimalNum);
            }
        }
    }
}

const int32_t &Oauth2UserConsents::getValueOfId() const noexcept
{
    static const int32_t defaultValue = int32_t();
    if(id_)
        return *id_;
    return defaultValue;
}
const std::shared_ptr<int32_t> &Oauth2UserConsents::getId() const noexcept
{
    return id_;
}
void Oauth2UserConsents::setId(const int32_t &pId) noexcept
{
    id_ = std::make_shared<int32_t>(pId);
    dirtyFlag_[0] = true;
}
const typename Oauth2UserConsents::PrimaryKeyType & Oauth2UserConsents::getPrimaryKey() const
{
    assert(id_);
    return *id_;
}

const int32_t &Oauth2UserConsents::getValueOfInternalUserId() const noexcept
{
    static const int32_t defaultValue = int32_t();
    if(internalUserId_)
        return *internalUserId_;
    return defaultValue;
}
const std::shared_ptr<int32_t> &Oauth2UserConsents::getInternalUserId() const noexcept
{
    return internalUserId_;
}
void Oauth2UserConsents::setInternalUserId(const int32_t &pInternalUserId) noexcept
{
    internalUserId_ = std::make_shared<int32_t>(pInternalUserId);
    dirtyFlag_[1] = true;
}

const std::string &Oauth2UserConsents::getValueOfClientId() const noexcept
{
    static const std::string defaultValue = std::string();
    if(clientId_)
        return *clientId_;
    return defaultValue;
}
const std::shared_ptr<std::string> &Oauth2UserConsents::getClientId() const noexcept
{
    return clientId_;
}
void Oauth2UserConsents::setClientId(const std::string &pClientId) noexcept
{
    clientId_ = std::make_shared<std::string>(pClientId);
    dirtyFlag_[2] = true;
}
void Oauth2UserConsents::setClientId(std::string &&pClientId) noexcept
{
    clientId_ = std::make_shared<std::string>(std::move(pClientId));
    dirtyFlag_[2] = true;
}
void Oauth2UserConsents::setClientIdToNull() noexcept
{
    clientId_.reset();
    dirtyFlag_[2] = true;
}

const std::string &Oauth2UserConsents::getValueOfScopeName() const noexcept
{
    static const std::string defaultValue = std::string();
    if(scopeName_)
        return *scopeName_;
    return defaultValue;
}
const std::shared_ptr<std::string> &Oauth2UserConsents::getScopeName() const noexcept
{
    return scopeName_;
}
void Oauth2UserConsents::setScopeName(const std::string &pScopeName) noexcept
{
    scopeName_ = std::make_shared<std::string>(pScopeName);
    dirtyFlag_[3] = true;
}
void Oauth2UserConsents::setScopeName(std::string &&pScopeName) noexcept
{
    scopeName_ = std::make_shared<std::string>(std::move(pScopeName));
    dirtyFlag_[3] = true;
}
void Oauth2UserConsents::setScopeNameToNull() noexcept
{
    scopeName_.reset();
    dirtyFlag_[3] = true;
}

const ::trantor::Date &Oauth2UserConsents::getValueOfGrantedAt() const noexcept
{
    static const ::trantor::Date defaultValue = ::trantor::Date();
    if(grantedAt_)
        return *grantedAt_;
    return defaultValue;
}
const std::shared_ptr<::trantor::Date> &Oauth2UserConsents::getGrantedAt() const noexcept
{
    return grantedAt_;
}
void Oauth2UserConsents::setGrantedAt(const ::trantor::Date &pGrantedAt) noexcept
{
    grantedAt_ = std::make_shared<::trantor::Date>(pGrantedAt);
    dirtyFlag_[4] = true;
}
void Oauth2UserConsents::setGrantedAtToNull() noexcept
{
    grantedAt_.reset();
    dirtyFlag_[4] = true;
}

void Oauth2UserConsents::updateId(const uint64_t id)
{
}

const std::vector<std::string> &Oauth2UserConsents::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
        "internal_user_id",
        "client_id",
        "scope_name",
        "granted_at"
    };
    return inCols;
}

void Oauth2UserConsents::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getInternalUserId())
        {
            binder << getValueOfInternalUserId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getClientId())
        {
            binder << getValueOfClientId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[3])
    {
        if(getScopeName())
        {
            binder << getValueOfScopeName();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[4])
    {
        if(getGrantedAt())
        {
            binder << getValueOfGrantedAt();
        }
        else
        {
            binder << nullptr;
        }
    }
}

const std::vector<std::string> Oauth2UserConsents::updateColumns() const
{
    std::vector<std::string> ret;
    if(dirtyFlag_[1])
    {
        ret.push_back(getColumnName(1));
    }
    if(dirtyFlag_[2])
    {
        ret.push_back(getColumnName(2));
    }
    if(dirtyFlag_[3])
    {
        ret.push_back(getColumnName(3));
    }
    if(dirtyFlag_[4])
    {
        ret.push_back(getColumnName(4));
    }
    return ret;
}

void Oauth2UserConsents::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getInternalUserId())
        {
            binder << getValueOfInternalUserId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getClientId())
        {
            binder << getValueOfClientId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[3])
    {
        if(getScopeName())
        {
            binder << getValueOfScopeName();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[4])
    {
        if(getGrantedAt())
        {
            binder << getValueOfGrantedAt();
        }
        else
        {
            binder << nullptr;
        }
    }
}
Json::Value Oauth2UserConsents::toJson() const
{
    Json::Value ret;
    if(getId())
    {
        ret["id"]=getValueOfId();
    }
    else
    {
        ret["id"]=Json::Value();
    }
    if(getInternalUserId())
    {
        ret["internal_user_id"]=getValueOfInternalUserId();
    }
    else
    {
        ret["internal_user_id"]=Json::Value();
    }
    if(getClientId())
    {
        ret["client_id"]=getValueOfClientId();
    }
    else
    {
        ret["client_id"]=Json::Value();
    }
    if(getScopeName())
    {
        ret["scope_name"]=getValueOfScopeName();
    }
    else
    {
        ret["scope_name"]=Json::Value();
    }
    if(getGrantedAt())
    {
        ret["granted_at"]=getGrantedAt()->toDbStringLocal();
    }
    else
    {
        ret["granted_at"]=Json::Value();
    }
    return ret;
}

std::string Oauth2UserConsents::toString() const
{
    return toJson().toStyledString();
}

Json::Value Oauth2UserConsents::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 5)
    {
        if(!pMasqueradingVector[0].empty())
        {
            if(getId())
            {
                ret[pMasqueradingVector[0]]=getValueOfId();
            }
            else
            {
                ret[pMasqueradingVector[0]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[1].empty())
        {
            if(getInternalUserId())
            {
                ret[pMasqueradingVector[1]]=getValueOfInternalUserId();
            }
            else
            {
                ret[pMasqueradingVector[1]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[2].empty())
        {
            if(getClientId())
            {
                ret[pMasqueradingVector[2]]=getValueOfClientId();
            }
            else
            {
                ret[pMasqueradingVector[2]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[3].empty())
        {
            if(getScopeName())
            {
                ret[pMasqueradingVector[3]]=getValueOfScopeName();
            }
            else
            {
                ret[pMasqueradingVector[3]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[4].empty())
        {
            if(getGrantedAt())
            {
                ret[pMasqueradingVector[4]]=getGrantedAt()->toDbStringLocal();
            }
            else
            {
                ret[pMasqueradingVector[4]]=Json::Value();
            }
        }
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    if(getId())
    {
        ret["id"]=getValueOfId();
    }
    else
    {
        ret["id"]=Json::Value();
    }
    if(getInternalUserId())
    {
        ret["internal_user_id"]=getValueOfInternalUserId();
    }
    else
    {
        ret["internal_user_id"]=Json::Value();
    }
    if(getClientId())
    {
        ret["client_id"]=getValueOfClientId();
    }
    else
    {
        ret["client_id"]=Json::Value();
    }
    if(getScopeName())
    {
        ret["scope_name"]=getValueOfScopeName();
    }
    else
    {
        ret["scope_name"]=Json::Value();
    }
    if(getGrantedAt())
    {
        ret["granted_at"]=getGrantedAt()->toDbStringLocal();
    }
    else
    {
        ret["granted_at"]=Json::Value();
    }
    return ret;
}

bool Oauth2UserConsents::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("id"))
    {
        if(!validJsonOfField(0, "id", pJson["id"], err, true))
            return false;
    }
    if(pJson.isMember("internal_user_id"))
    {
        if(!validJsonOfField(1, "internal_user_id", pJson["internal_user_id"], err, true))
            return false;
    }
    else
    {
        err="The internal_user_id column cannot be null";
        return false;
    }
    if(pJson.isMember("client_id"))
    {
        if(!validJsonOfField(2, "client_id", pJson["client_id"], err, true))
            return false;
    }
    if(pJson.isMember("scope_name"))
    {
        if(!validJsonOfField(3, "scope_name", pJson["scope_name"], err, true))
            return false;
    }
    if(pJson.isMember("granted_at"))
    {
        if(!validJsonOfField(4, "granted_at", pJson["granted_at"], err, true))
            return false;
    }
    return true;
}
bool Oauth2UserConsents::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                            const std::vector<std::string> &pMasqueradingVector,
                                                            std::string &err)
{
    if(pMasqueradingVector.size() != 5)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty())
      {
          if(pJson.isMember(pMasqueradingVector[0]))
          {
              if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[1].empty())
      {
          if(pJson.isMember(pMasqueradingVector[1]))
          {
              if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[1] + " column cannot be null";
            return false;
        }
      }
      if(!pMasqueradingVector[2].empty())
      {
          if(pJson.isMember(pMasqueradingVector[2]))
          {
              if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[3].empty())
      {
          if(pJson.isMember(pMasqueradingVector[3]))
          {
              if(!validJsonOfField(3, pMasqueradingVector[3], pJson[pMasqueradingVector[3]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[4].empty())
      {
          if(pJson.isMember(pMasqueradingVector[4]))
          {
              if(!validJsonOfField(4, pMasqueradingVector[4], pJson[pMasqueradingVector[4]], err, true))
                  return false;
          }
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Oauth2UserConsents::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("id"))
    {
        if(!validJsonOfField(0, "id", pJson["id"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    if(pJson.isMember("internal_user_id"))
    {
        if(!validJsonOfField(1, "internal_user_id", pJson["internal_user_id"], err, false))
            return false;
    }
    if(pJson.isMember("client_id"))
    {
        if(!validJsonOfField(2, "client_id", pJson["client_id"], err, false))
            return false;
    }
    if(pJson.isMember("scope_name"))
    {
        if(!validJsonOfField(3, "scope_name", pJson["scope_name"], err, false))
            return false;
    }
    if(pJson.isMember("granted_at"))
    {
        if(!validJsonOfField(4, "granted_at", pJson["granted_at"], err, false))
            return false;
    }
    return true;
}
bool Oauth2UserConsents::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                                          const std::vector<std::string> &pMasqueradingVector,
                                                          std::string &err)
{
    if(pMasqueradingVector.size() != 5)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
      {
          if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
      if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
      {
          if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, false))
              return false;
      }
      if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
      {
          if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, false))
              return false;
      }
      if(!pMasqueradingVector[3].empty() && pJson.isMember(pMasqueradingVector[3]))
      {
          if(!validJsonOfField(3, pMasqueradingVector[3], pJson[pMasqueradingVector[3]], err, false))
              return false;
      }
      if(!pMasqueradingVector[4].empty() && pJson.isMember(pMasqueradingVector[4]))
      {
          if(!validJsonOfField(4, pMasqueradingVector[4], pJson[pMasqueradingVector[4]], err, false))
              return false;
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Oauth2UserConsents::validJsonOfField(size_t index,
                                          const std::string &fieldName,
                                          const Json::Value &pJson,
                                          std::string &err,
                                          bool isForCreation)
{
    switch(index)
    {
        case 0:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(isForCreation)
            {
                err="The automatic primary key cannot be set";
                return false;
            }
            if(!pJson.isInt())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 1:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isInt())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 2:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            if(pJson.isString() && std::wstring_convert<std::codecvt_utf8_utf16<wchar_t>, wchar_t>{}
                .from_bytes(pJson.asCString()).size() > 50)
            {
                err="String length exceeds limit for the " +
                    fieldName +
                    " field (the maximum value is 50)";
                return false;
            }
            break;
        case 3:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            if(pJson.isString() && std::wstring_convert<std::codecvt_utf8_utf16<wchar_t>, wchar_t>{}
                .from_bytes(pJson.asCString()).size() > 100)
            {
                err="String length exceeds limit for the " +
                    fieldName +
                    " field (the maximum value is 100)";
                return false;
            }
            break;
        case 4:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
Users Oauth2UserConsents::getUsers(const DbClientPtr &clientPtr) const {
    static const std::string sql = "select * from users where id = $1";
    Result r(nullptr);
    {
        auto binder = *clientPtr << sql;
        binder << *internalUserId_ << Mode::Blocking >>
            [&r](const Result &result) { r = result; };
        binder.exec();
    }
    if (r.size() == 0)
    {
        throw UnexpectedRows("0 rows found");
    }
    else if (r.size() > 1)
    {
        throw UnexpectedRows("Found more than one row");
    }
    return Users(r[0]);
}

void Oauth2UserConsents::getUsers(const DbClientPtr &clientPtr,
                                  const std::function<void(Users)> &rcb,
                                  const ExceptionCallback &ecb) const
{
    static const std::string sql = "select * from users where id = $1";
    *clientPtr << sql
               << *internalUserId_
               >> [rcb = std::move(rcb), ecb](const Result &r){
                    if (r.size() == 0)
                    {
                        ecb(UnexpectedRows("0 rows found"));
                    }
                    else if (r.size() > 1)
                    {
                        ecb(UnexpectedRows("Found more than one row"));
                    }
                    else
                    {
                        rcb(Users(r[0]));
                    }
               }
               >> ecb;
}
Oauth2Clients Oauth2UserConsents::getOauth2Clients(const DbClientPtr &clientPtr) const {
    static const std::string sql = "select * from oauth2_clients where client_id = $1";
    Result r(nullptr);
    {
        auto binder = *clientPtr << sql;
        binder << *clientId_ << Mode::Blocking >>
            [&r](const Result &result) { r = result; };
        binder.exec();
    }
    if (r.size() == 0)
    {
        throw UnexpectedRows("0 rows found");
    }
    else if (r.size() > 1)
    {
        throw UnexpectedRows("Found more than one row");
    }
    return Oauth2Clients(r[0]);
}

void Oauth2UserConsents::getOauth2Clients(const DbClientPtr &clientPtr,
                                          const std::function<void(Oauth2Clients)> &rcb,
                                          const ExceptionCallback &ecb) const
{
    static const std::string sql = "select * from oauth2_clients where client_id = $1";
    *clientPtr << sql
               << *clientId_
               >> [rcb = std::move(rcb), ecb](const Result &r){
                    if (r.size() == 0)
                    {
                        ecb(UnexpectedRows("0 rows found"));
                    }
                    else if (r.size() > 1)
                    {
                        ecb(UnexpectedRows("Found more than one row"));
                    }
                    else
                    {
                        rcb(Oauth2Clients(r[0]));
                    }
               }
               >> ecb;
}
Oauth2Scopes Oauth2UserConsents::getOauth2Scopes(const DbClientPtr &clientPtr) const {
    static const std::string sql = "select * from oauth2_scopes where name = $1";
    Result r(nullptr);
    {
        auto binder = *clientPtr << sql;
        binder << *scopeName_ << Mode::Blocking >>
            [&r](const Result &result) { r = result; };
        binder.exec();
    }
    if (r.size() == 0)
    {
        throw UnexpectedRows("0 rows found");
    }
    else if (r.size() > 1)
    {
        throw UnexpectedRows("Found more than one row");
    }
    return Oauth2Scopes(r[0]);
}

void Oauth2UserConsents::getOauth2Scopes(const DbClientPtr &clientPtr,
                                         const std::function<void(Oauth2Scopes)> &rcb,
                                         const ExceptionCallback &ecb) const
{
    static const std::string sql = "select * from oauth2_scopes where name = $1";
    *clientPtr << sql
               << *scopeName_
               >> [rcb = std::move(rcb), ecb](const Result &r){
                    if (r.size() == 0)
                    {
                        ecb(UnexpectedRows("0 rows found"));
                    }
                    else if (r.size() > 1)
                    {
                        ecb(UnexpectedRows("Found more than one row"));
                    }
                    else
                    {
                        rcb(Oauth2Scopes(r[0]));
                    }
               }
               >> ecb;
}
